[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tillm"
version = "0.1.44"
description = "Text-interface LLM control plane for semcod/coru shell automation (pair with gillm for GUI)."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Semcod" }, { name = "Tom Sapletta", email = "tom@sapletta.com" }]
license = "Apache-2.0"
keywords = ["llm", "shell", "aider", "claude", "codex", "automation"]
dependencies = []

[project.optional-dependencies]
env = ["env2llm>=0.1.12"]
planfile = ["planfile>=0.1.117"]
dev = [
    "build>=1.0,<2.0",
    "pytest>=8.0,<10.0",
    "ruff>=0.11,<0.16",
    "twine>=6.0,<7.0",
    "goal>=2.1.0",
    "costs>=0.1.20",
    "testql>=1.2.64; python_version < '3.14'",
    "env2llm>=0.1.12",
]

[project.scripts]
tillm = "tillm.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
# "tillm" alone excludes subpackages (tillm.transports shipped broken wheels);
# the wildcard picks up every subpackage.
include = ["tillm", "tillm.*"]

[tool.pytest.ini_options]
addopts = "-p wellmanifest_governance"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.costs]
# AI Cost tracking configuration
badge = true
update_readme = true
readme_path = "README.md"
default_model = "openrouter/deep/deep-v4-pro"
analysis_mode = "byok"
full_history = true
max_commits = 500

# Cost thresholds for badge colors (USD)
badge_color_thresholds = { low = 1.0, medium = 5.0, high = 10.0, critical = 50.0 }

[tool.wellmanifest]
standard = "0.20.35"
revision = "cfaa0bf0ea6b0e7349fed0bb62b5ce15792d687d"
gate = "project/governance-check.sh"
